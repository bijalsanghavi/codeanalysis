# codeanalysis

`test-download.zip` is a connectivity check: can a managed Abbott laptop download
a file from a public GitHub repository?

Download it and unzip it. If you get a picture, the path works and this repo can
carry the migration tooling. If it 404s or will not load, GitHub is blocked for
that machine and the tooling has to travel another way.
